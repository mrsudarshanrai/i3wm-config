[Unit]
Description=Watch battery percentage and notify if less than 20%
After=network.target

[Service]
Type=simple
ExecStart=%h/.config/scripts/batteryWatcher.sh
WorkingDirectory=/tmp

[Install]
WantedBy=default.target
