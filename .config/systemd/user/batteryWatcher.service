[Unit]
Description=Watch battery percentage and notify if less than 20%
After=network.target

[Service]
Type=simple
ExecStart=/home/fox/.config/scripts/batteryWatcher.sh
WorkingDirectory=/tmp

[Install]
WantedBy=default.target
